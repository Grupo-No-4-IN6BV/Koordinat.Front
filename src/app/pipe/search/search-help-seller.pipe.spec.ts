import { SearchHelpSellerPipe } from './search-help-seller.pipe';

describe('SearchHelpSellerPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchHelpSellerPipe();
    expect(pipe).toBeTruthy();
  });
});
